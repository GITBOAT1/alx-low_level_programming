#include "lists.h"
/**
 * print_list - this print nodes
 * @h: the node for printing.
 * Return: Always 0.
 */

size_t print_list(const list_t *h)
{
	l = 0;
	while (h != NULL)
	{
		printf("%s", h->str;);
		h = h->next;
	}
	return (head->len);
}
