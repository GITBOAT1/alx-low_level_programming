#include "3-calc.h"

/**
 * main - check the code
 * @argc:
 * @argv:
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	if(!argv || argc != 3)
	{
		printf("Error\n");
		return (98);
	}
	int num1, num2, sum;
	char *oprator;

	num1 = atoi(argc[1]);
	num2 = atoi(argc[3])
}
