#include "function_pointers.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

void array_iterator(int *array, size_t size, void (*action)(int))
{
	int l;
	for (l = 0; l < size; l++)
	{
		action(array[l]);
	}
}
