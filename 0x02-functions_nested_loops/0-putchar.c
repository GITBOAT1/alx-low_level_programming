#include <stdio.h>

/**
 * main - This is where the program begins
 * @void: This shows its empty
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int main(void)
{
	printf("_putchar\n");
	return (0);
}
