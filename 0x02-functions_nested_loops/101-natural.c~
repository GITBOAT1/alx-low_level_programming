#include <stdio.h>

/**
 * main - print out multiples of 3 or 5 below 1024
 *
 * Return: 0;
 */

int main(void)
{
	int count = 1024, three = 3, five = 5, i;

	for (i = 0; i < 1024; i ++)
	{
		if (( i % three == 0) || (i % five == 0))
		{
			printf(i);
		}
	}
}
